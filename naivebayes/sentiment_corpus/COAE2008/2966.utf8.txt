使用者仍在自己原先习惯的环境下“照常工作”，编写文件，完成后选择“打印成DynaDoc文件”，就自动形成后缀名为．WDL的可携式文件。这时，文字资料已被嵌入文件，不仅记存文字外形，同时保持原有字码。DynaDoc格式的文件并不大，以邓小平文选第三卷（264页）为例，嵌入中文字形的DynaDoc文件为1M，可脱离中文系统在打印机以至照排机上完美输出。而以Excel、AutoCAD等软件制作的文档用DynaDoc打包后因略去了格式等信息，一般反而小于原来文件。